package com.theshuai.specusserver.management.repository;

import com.theshuai.specusserver.management.model.PeerMeshServiceSharing;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PeerMeshServiceSharingRepository extends JpaRepository<PeerMeshServiceSharing, String> {
}
