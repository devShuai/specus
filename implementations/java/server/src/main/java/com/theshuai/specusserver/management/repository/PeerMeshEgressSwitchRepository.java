package com.theshuai.specusserver.management.repository;

import com.theshuai.specusserver.management.model.PeerMeshEgressSwitch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PeerMeshEgressSwitchRepository extends JpaRepository<PeerMeshEgressSwitch, String> {
}
