# Protocol Test Vectors

Binary and JSON fixtures used to prove cross-language compatibility live here.

Existing fixtures are still under `implementations/csharp/protocol/tests/fixtures`
until the protocol suite is consolidated. New shared fixtures should be added
here and referenced by each implementation's tests.
