# Protocol Schemas

JSON schemas for cross-language control messages live here.

Planned schemas:

- `nat-control.schema.json`
- `peer-control.schema.json`
- `client-auth-login.schema.json`

Keep schemas language-neutral; generated code should live under each
implementation, not in this directory.
